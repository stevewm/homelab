# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/kustomize.toolkit.fluxcd.io/kustomization_v1.json
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: app-mealie
  namespace: &namespace selfhosted
spec:
  targetNamespace: *namespace
  commonMetadata:
    labels:
      app.kubernetes.io/name: &app mealie
  interval: 30m
  timeout: 5m
  path: ./cluster/kubernetes/apps/selfhosted/mealie/app
  prune: false
  sourceRef:
    kind: GitRepository
    name: homelab
    namespace: flux-system
  wait: true
  components:
    - ../../../../components/volsync/hetzner
    - ../../../../components/sso-secret
  postBuild:
    substitute:
      APP: *app
      VOLSYNC_CLAIM: mealie-config
