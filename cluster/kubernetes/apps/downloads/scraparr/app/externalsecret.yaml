# yaml-language-server: $schema=https://crds.stevewm.dev/crds/external-secrets.io/externalsecret_v1.json
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: scraparr
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: doppler
  target:
    name: scraparr
  data:
    - secretKey: seerr_api_key
      remoteRef:
        key: SEERR_API_KEY
