# yaml-language-server: $schema=https://crds.stevewm.dev/crds/external-secrets.io/clustersecretstore_v1.json
---
apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: doppler
spec:
  provider:
    doppler:
      auth:
        secretRef:
          dopplerToken:
            namespace: security
            name: doppler
            key: token
