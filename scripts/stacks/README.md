# Stack Deployer

Deploys newly defined stacks to Portainer. Does not handle deletion of stacks, but will warm of orphaned stacks.

## Configuration

Environment variables are used to configure the deployer:
