[project]
name = "homelab-scripts"
version = "0.1.0"
description = "homelab python scripts"
readme = "../docs/README.md"
requires-python = ">=3.13"
dependencies = [
    "black>=24.10.0",
    "pyyaml>=6.0.2",
]
