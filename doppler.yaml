---
setup:
  - project: talos
    path: cluster/talos
    config: prd

  - project: homelab
    path: cluster/kubernetes
    config: prd

  - project: homelab
    path: cluster/bootstrap
    config: prd
