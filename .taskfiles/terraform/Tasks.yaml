---
# https://taskfile.dev

version: '3'

tasks:
  bootstrap:
      desc: Bootstrap Terraform Cloud
      dir: terraform/tf-tfc
      cmds:
        - terraform apply -var="terraform_cloud_token=${TFC_TOKEN}" -var="github_pat=${TFC_BOOTSTRAP_GITHUB_TOKEN}"
