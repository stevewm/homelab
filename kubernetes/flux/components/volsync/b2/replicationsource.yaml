# yaml-language-server: $schema=https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/volsync.backube/replicationsource_v1alpha1.json
---
apiVersion: volsync.backube/v1alpha1
kind: ReplicationSource
metadata:
  name: ${APP}-backup
spec:
  sourcePVC: ${VOLSYNC_CLAIM:-${APP}}
  trigger:
    schedule: ${VOLSYNC_SCHEDULE:-"0 */12 * * *"}
  restic:
    repository: ${APP}-volsync-b2
    copyMethod: ${VOLSYNC_COPY_METHOD:-Snapshot}
    storageClassName: ${VOLSYNC_STORAGECLASS:-csi-local-hostpath}
    capacity: ${VOLSYNC_CAPACITY:-1Gi}
    accessModes: ["${VOLSYNC_ACCESSMODES:-ReadWriteOnce}"]
    volumeSnapshotClassName: ${VOLSYNC_STORAGECLASS:-csi-local-hostpath}
    moverSecurityContext:
      runAsUser: ${APP_UID:-568}
      runAsGroup: ${APP_GID:-568}
      fsGroup: ${APP_GID:-568}
    pruneIntervalDays: 7
    retain:
      hourly: 24
      daily: 7
      weekly: 5
