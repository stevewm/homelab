---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: jellystat-pg-db
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: crunchy-pgo-secrets
  target:
    name: jellystat-pg-db
  dataFrom:
    - extract:
        key: postgres-pguser-jellystat
