---
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: playit
spec:
  endpoints:
    - dnsName: play.cfg.sh
      recordType: A
      targets: [''] # TOOD: Replace with private IP from playit when in stock
