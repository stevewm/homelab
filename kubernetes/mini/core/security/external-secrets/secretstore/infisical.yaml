---
apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: passbolt
spec:
  provider:
    infisical:
      host: http://infisical.security.svc.cluster.local
      auth:
        secretRef:
          token:
            namespace: security
            name: doppler-token
            key: token
