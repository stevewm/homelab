[api]
    enabled = true
    tls = false
    listen = "0.0.0.0:80"

[backend]
    datastore = "config"
    baseDN = "dc=home,dc=arpa"

[ldap]
    enabled = true
    listen = "0.0.0.0:389"

[ldaps]
    enabled = false
