# yaml-language-server: $schema=https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/external-secrets.io/externalsecret_v1beta1.json
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: authelia-db
spec:
  refreshInterval: 1m
  secretStoreRef:
    name: crunchy-pgo-secrets
    kind: ClusterSecretStore
  target:
    name: authelia-db
    template:
      type: Opaque
      data:
        AUTHELIA_STORAGE_POSTGRES_ADDRESS: '{{ index . "pgbouncer-host" }}'
        AUTHELIA_STORAGE_POSTGRES_DATABASE: '{{ index . "dbname" }}'
        AUTHELIA_STORAGE_POSTGRES_USERNAME: '{{ index . "user" }}'
        AUTHELIA_STORAGE_POSTGRES_PASSWORD: '{{ index . "password" }}'
  dataFrom:
    - extract:
        key: postgres-pguser-authelia

# yaml-language-server: $schema=https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/external-secrets.io/externalsecret_v1beta1.json
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: authelia-oidc
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: doppler
  target:
    name: authelia
    creationPolicy: Merge
  data:
    - secretKey: AUTHELIA_IDENTITY_PROVIDERS_OIDC_JWKS
      remoteRef:
        key: AUTHELIA_OIDC_ISSUER_PRIVATE_KEY
    - secretKey: AUTHELIA_AUTHENTICATION_BACKEND_LDAP_PASSWORD
      remoteRef:
        key: GLAUTH_SEARCH_PSW
