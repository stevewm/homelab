# yaml-language-server: $schema=https://json.schemastore.org/kustomization.json
---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./l2.yaml
  - ./pool.yaml
  # Network policies
  # Egress
  - ./netpols/allow-egress-apiserver.yaml
  - ./netpols/allow-egress-kubedns.yaml
  - ./netpols/allow-egress-internet.yaml
  - ./netpols/allow-egress-nas.yaml
  # Ingress
  - ./netpols/allow-ingress-apiserver.yaml
  - ./netpols/allow-ingress-lan.yaml
# - ./netpols/default-allow-egress-kubedns.yaml # TODO: reinstate once other netpols are working
