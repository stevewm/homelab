# yaml-language-server: $schema=https://json.schemastore.org/kustomization.json
---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./cyberchef/ks.yaml
  - ./homepage/ks.yaml
  - ./stirling-pdf/ks.yaml
  - ./themepark/ks.yaml
components:
  - ../../components/namespace
  - ../../components/networkpolicy/default-deny
patches:
  - patch: |
      - op: add
        path: /metadata/name
        value: selfhosted
    target:
      kind: Namespace
