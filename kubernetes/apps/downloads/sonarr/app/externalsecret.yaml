---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: sonarr
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: cluster-secrets
  target:
    name: sonarr
  data:
    - secretKey: api_key
      remoteRef:
        key: SONARR_API_KEY
